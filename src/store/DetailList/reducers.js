const initialState = {
  title: '',
  image: '',
  isLoading: '',
};

const Reducers = (state = initialState, action) => {
  //   const { type, payload } = action;

  return state;
};

export default Reducers;
